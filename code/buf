
: BufClear (addr) 0 swap c! ;

: BufAdd (val addr -- )
	dup                 ( val addr addr )
	dup                 ( val addr addr addr )
	c@                  ( val addr addr length )
	+                   ( val addr offset )
	1+                  ( val addr offset )
	2 pick              ( val addr offset val )
	swap                ( val addr val offset )
	c!                  ( val addr )
	swap                ( addr val )
	drop                ( addr )
	dup                 ( addr addr )
	c@                  ( addr length )
	1+                  ( addr updatedLength )
	swap                ( updatedLength addr )
	c!                  (  )
;


